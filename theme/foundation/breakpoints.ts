import { createBreakpoints } from '@chakra-ui/theme-tools';

const breakpoints = {
  '2xs': '22em',
  xs: '26em',
  sm: '30em',
  md: '48em',
  lg: '62em',
  xl: '80em',
  '2xl': '96em',
}

export default createBreakpoints(breakpoints);